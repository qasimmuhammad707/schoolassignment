version: '3.8'

services:
  # 1. Backend Container (Direct MongoDB Atlas Cloud se connect hoga)
  app-backend:
    build: ./backend
    container_name: school-backend
    ports:
      - "5000:5000"
    environment:
      # Hum ne password ki jagah seedha Qasim12345 likh diya hai
      - MONGO_URI=mongodb+srv://qasimjutt1416_db_user:Qasim12345@schoolcluster.phnwpnu.mongodb.net/schoolDB?retryWrites=true&w=majority&appName=schoolcluster
    restart: always

  # 2. Frontend Container
  app-frontend:
    build: ./frontend
    container_name: school-frontend
    ports:
      - "8080:80"
    restart: always
