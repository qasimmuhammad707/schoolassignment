version: '3.8'

services:
  # 1. Backend Container (Direct MongoDB Atlas Cloud se connect hoga)
  app-backend:
    build: ./backend
    container_name: school-backend
    ports:
      - "5000:5000"
    environment:
      # Hamein link ke andar database ka naam /schoolDB lazmi dena hota hai taake data sahi jagah jaye
      - MONGO_URI=mongodb+srv://qasimjutt1416_db_user:<db_password>@schoolcluster.phnwpnu.mongodb.net/schoolDB?retryWrites=true&w=majority&appName=schoolcluster
    restart: always

  # 2. Frontend Container
  app-frontend:
    build: ./frontend
    container_name: school-frontend
    ports:
      - "8080:80"
    restart: always
